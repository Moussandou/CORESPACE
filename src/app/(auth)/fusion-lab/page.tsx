export default function FusionLabPage() {
    return (
        <main className="flex min-h-screen items-center justify-center">
            <p className="font-mono text-sm" style={{ color: 'var(--color-text-muted)' }}>
                [ FUSION LAB — placeholder ]
            </p>
        </main>
    );
}
